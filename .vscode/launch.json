{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Executable",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "program": "${workspaceRoot}/goshort",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "Build"
        }
    ]
}